pipeline {
  agent any
  stages {
    stage('buil') {
      steps {
        echo 'build'
      }
    }

    stage('upload') {
      steps {
        echo 'upload'
      }
    }

    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }

  }
}